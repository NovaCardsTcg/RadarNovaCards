# Copia este fichero como .env y rellena los dos valores.
# El .env esta en .gitignore: no se sube nunca al repositorio.
#
# Son los mismos valores que pusiste en los secrets de GitHub.

TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
